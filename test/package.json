{
    "name": "xdebug-helper-test",
    "version": "1.0.3",
    "scripts": {
        "test": "jest",
        "watch": "jest --watch",
        "test-options": "jest -t 'Options Tests'",
        "test-popup": "jest -t 'Popup Tests'",
        "test-combined": "jest -t 'Combined Tests'"
    },
    "dependencies": {
        "jest": "^29.7.0",
        "puppeteer-core": "^24.2.1"
    },
    "overrides": {
        "glob": "10.0.0"
    }
}